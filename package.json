{
  "name": "taisho",
  "version": "0.0.0",
  "private": true,
  "author": "chuross",
  "description": "A simple helpful robot for your Company",
  "scripts": {
    "start": "bin/hubot",
    "start:line": "bin/hubot -a line-messaging",
    "compile": "tsc scripts-ts/*.ts --outDir scripts",
    "sync": "git push origin master && git push heroku master"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "hubot": "^2.19.0",
    "hubot-heroku-keepalive": "^1.0.3",
    "hubot-line-messaging": "^0.6.0"
  },
  "engines": {
    "node": "8.x.x"
  }
}
